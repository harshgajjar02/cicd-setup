services:
  jenkins:
    image: jenkins/jenkins
    stop_grace_period: 2m
    ports:
      - "8080:8080"
      - "5000:5000"
    volumes:
      - jenkins_home:/var/jenkins_home
  ssh-agent:
    image: jenkins/ssh-agent
    
volumes:
  jenkins_home: {}
